{
  "scripts": {
    "publish": "build --mac --win -p always",
    "//": "build -p always"
  },
  "dependencies": {
    "electron-log": "^1.3.0",
    "electron-updater": "^2.21.4"
  },
  "devDependencies": {
    "electron": "^1.7.9",
    "electron-builder": "^20.8.2"
  }
}
